

fun add(nums: (Int::2)): (Int::3)
{
    => (nums[0], nums[1], nums[0] + nums[1])
}


val result = add((10, 20))

push "\n\n=========="
push "${result[0]} + ${result[1]} = ${result[2]}"
push "==========\n\n"
